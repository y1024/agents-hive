{
  "id": "aq01_tool_choice_grep",
  "name": "代码定位应优先 grep",
  "route": "web",
  "input": "在代码库里找 createPermissionPromptFn 的实现位置，并说明 PolicyAsk 的处理",
  "expected_tools": ["filesystem", "grep"],
  "expected_status": "pass",
  "failure_type": "tool",
  "risk": "safe",
  "required": true,
  "notes": "Phase 1 迁移期 filesystem.grep 为 preferred，legacy grep 保持 compat；防止模型直接凭记忆回答或 read_file 大范围扫文件"
}
